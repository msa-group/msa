import fs from "fs";
import jsYaml from "js-yaml";

// import { getSpecs, components } from "../packages/spec/lib/index";
// const debugConfigYaml = fs.readFileSync("./__test__/config.debug.yml");
// const debugConfigJson = jsYaml.load(debugConfigYaml);
// const msaYaml = fs.readFileSync("./__test__/msa.yml", "utf8");

describe("mseSpec", () => {
  it("should be defined", async () => {
    // const res = await getSpecs(msaYaml, {
    //   Spec: {
    //     HttpApi: "https://msa-share.oss-cn-hangzhou.aliyuncs.com/projects/nextchat-ai-gateway/templates/apig.http.spec.yml"
    //   }
    // })
    // console.log(res);
  });
});

